-- PHAN 1: KHAI BAO BIEN VA SETTINGS
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local Settings = {
    Aimlock = false,
    FOVCircle = false,
    ESP = false,
    ESPName = false,
    ESPHealth = false,
    ESPDistance = false,
    ESPTeam = false,
    ShowTeammates = false,
    IgnoreTeammates = false,
    IgnoreWalls = false,
    AimPart = "Head",
    FOVRadius = 150,
    AimSmoothness = 0.7,
    NoRecoil = false,
    InfiniteAmmo = false,
    Triggerbot = false,
    TargetMode = "ClosestDistance",
    RandomOffset = false,
    OffsetMax = 0.3,
}

local MenuOpen = true
local Dragging = false
local DragStart, DragOffset
local Target
local ESPCache = {}

local FOVCircle
local FOVGlow
local FOVLabel

-- PHAN 2: CAC HAM TIEN ICH (IsAlive, IsTeammate, IsVisible, GetRandomOffset, GetTargets)
local function IsAlive(character)
    if not character then return false end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    return hrp and humanoid and humanoid.Health > 0
end

local function GetTeam(player)
    return player.Team
end

local function IsTeammate(player)
    if player == LocalPlayer then return false end
    local myTeam = GetTeam(LocalPlayer)
    local theirTeam = GetTeam(player)
    return myTeam and theirTeam and myTeam == theirTeam
end

local rayParams = RaycastParams.new()
rayParams.FilterType = Enum.RaycastFilterType.Blacklist
rayParams.IgnoreWater = true

local function IsVisible(character)
    if not character then return false end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return false end
    local origin = Camera.CFrame.Position
    local direction = hrp.Position - origin
    rayParams.FilterDescendantsInstances = {LocalPlayer.Character, character}
    local result = workspace:Raycast(origin, direction, rayParams)
    if not result then return true end
    return result.Instance:IsDescendantOf(character)
end

local function GetRandomOffset()
    if not Settings.RandomOffset then return Vector3.new(0, 0, 0) end
    return Vector3.new(
        math.random(-Settings.OffsetMax * 10, Settings.OffsetMax * 10) / 10,
        math.random(-Settings.OffsetMax * 10, Settings.OffsetMax * 10) / 10,
        math.random(-Settings.OffsetMax * 10, Settings.OffsetMax * 10) / 10
    )
end

local function GetTargets()
    local targets = {}
    local center = Camera.ViewportSize / 2
    local fovRadius = Settings.FOVCircle and Settings.FOVRadius or math.huge

    for _, plr in pairs(Players:GetPlayers()) do
        if plr == LocalPlayer then continue end
        if Settings.IgnoreTeammates and IsTeammate(plr) then continue end
        local character = plr.Character
        if not IsAlive(character) then continue end

        local hrp = character:FindFirstChild("HumanoidRootPart")
        if not hrp then continue end

        local targetPos
        if Settings.AimPart == "Head" then
            local head = character:FindFirstChild("Head")
            targetPos = head and head.Position or hrp.Position
        else
            targetPos = hrp.Position
        end

        local screenPos, onScreen = Camera:WorldToViewportPoint(targetPos)
        if not onScreen then continue end

        if not Settings.IgnoreWalls and not IsVisible(character) then continue end

        local dist = (Vector2.new(screenPos.X, screenPos.Y) - center).Magnitude
        if dist > fovRadius then continue end

        local humanoid = character:FindFirstChildOfClass("Humanoid")
        table.insert(targets, {
            Player = plr,
            Character = character,
            HRP = hrp,
            Part = targetPos,
            ScreenPos = screenPos,
            Distance = (Camera.CFrame.Position - targetPos).Magnitude,
            HP = humanoid and humanoid.Health or 0,
            ScreenDist = dist,
        })
    end

    if #targets == 0 then return nil end

    if Settings.TargetMode == "LowestHP" then
        table.sort(targets, function(a, b) return a.HP < b.HP end)
    elseif Settings.TargetMode == "HighestHP" then
        table.sort(targets, function(a, b) return a.HP > b.HP end)
    else
        table.sort(targets, function(a, b) return a.ScreenDist < b.ScreenDist end)
    end

    return targets[1]
end

-- PHAN 3: CAC HAM NO RECOIL, INFINITE AMMO, TRIGGERBOT
local function NoRecoil()
    local char = LocalPlayer.Character
    if not char then return end
    local tool = char:FindFirstChildOfClass("Tool")
    if not tool then return end

    local function scan(obj)
        for _, v in pairs(obj:GetDescendants()) do
            if v:IsA("NumberValue") or v:IsA("IntValue") or v:IsA("FloatValue") then
                local name = v.Name:lower()
                if name:find("recoil") or name:find("kick") or name:find("shake") or name:find("spread") or name:find("bounce") then
                    v.Value = 0
                end
            end
            if v:IsA("CFrameValue") then
                local name = v.Name:lower()
                if name:find("recoil") or name:find("kick") or name:find("shake") then
                    v.Value = CFrame.new()
                end
            end
            if v:IsA("Vector3Value") then
                local name = v.Name:lower()
                if name:find("recoil") or name:find("kick") or name:find("shake") then
                    v.Value = Vector3.new(0, 0, 0)
                end
            end
        end
    end

    scan(tool)
    scan(char)
end

local function InfiniteAmmo()
    local char = LocalPlayer.Character
    if not char then return end
    local tool = char:FindFirstChildOfClass("Tool")
    if not tool then return end

    local function scan(obj)
        for _, v in pairs(obj:GetDescendants()) do
            if v:IsA("NumberValue") or v:IsA("IntValue") or v:IsA("FloatValue") then
                local name = v.Name:lower()
                if name:find("ammo") or name:find("clip") or name:find("mag") or name:find("bullet") or name:find("reserve") or name:find("max") then
                    v.Value = 999
                end
            end
        end
    end

    scan(tool)
    scan(char)
end

local function Triggerbot()
    if not Settings.Triggerbot then return end
    local target = GetTargets()
    if target then
        mouse1click()
    end
end

-- PHAN 4: TAO SCREENGUI VA MENU
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

local Menu = Instance.new("Frame")
Menu.Size = UDim2.new(0, 280, 0, 460)
Menu.Position = UDim2.new(0.5, -140, 0.5, -230)
Menu.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Menu.BackgroundTransparency = 0.1
Menu.BorderSizePixel = 0
Menu.Active = true
Menu.Draggable = true
Menu.Parent = ScreenGui

local MenuCorner = Instance.new("UICorner")
MenuCorner.CornerRadius = UDim.new(0, 8)
MenuCorner.Parent = Menu

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 30)
TitleBar.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
TitleBar.Parent = Menu

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 8)
TitleCorner.Parent = TitleBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -60, 1, 0)
TitleLabel.Position = UDim2.new(0, 10, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "AIM MENU"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 18
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TitleBar

local ToggleMenuBtn = Instance.new("TextButton")
ToggleMenuBtn.Size = UDim2.new(0, 30, 0, 30)
ToggleMenuBtn.Position = UDim2.new(1, -60, 0, 0)
ToggleMenuBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
ToggleMenuBtn.Text = "_"
ToggleMenuBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleMenuBtn.TextSize = 18
ToggleMenuBtn.Font = Enum.Font.GothamBold
ToggleMenuBtn.Parent = TitleBar

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 8)
ToggleCorner.Parent = ToggleMenuBtn

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -30, 0, 0)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 18
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = TitleBar

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseBtn

-- PHAN 5: TAO SCROLLING FRAME VA CAC HAM TAO TOGGLE/SELECTOR
local Scroll = Instance.new("ScrollingFrame")
Scroll.Size = UDim2.new(1, 0, 1, -30)
Scroll.Position = UDim2.new(0, 0, 0, 30)
Scroll.BackgroundTransparency = 1
Scroll.BorderSizePixel = 0
Scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
Scroll.ScrollBarThickness = 6
Scroll.Parent = Menu

local Layout = Instance.new("UIListLayout")
Layout.Padding = UDim.new(0, 5)
Layout.SortOrder = Enum.SortOrder.LayoutOrder
Layout.Parent = Scroll

local function CreateToggle(parent, text, settingKey, default)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 30)
    frame.BackgroundTransparency = 1
    frame.Parent = parent

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(220, 220, 220)
    label.TextSize = 14
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham
    label.Parent = frame

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 50, 1, -4)
    btn.Position = UDim2.new(1, -50, 0, 2)
    btn.BackgroundColor3 = default and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(180, 0, 0)
    btn.Text = default and "ON" or "OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 14
    btn.Font = Enum.Font.GothamBold
    btn.Parent = frame

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 4)
    btnCorner.Parent = btn

    Settings[settingKey] = default

    btn.MouseButton1Click:Connect(function()
        local newState = not Settings[settingKey]
        Settings[settingKey] = newState
        btn.Text = newState and "ON" or "OFF"
        btn.BackgroundColor3 = newState and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(180, 0, 0)
        if settingKey == "ESP" then
            UpdateESP()
        end
    end)

    return frame
end

local function CreateSelector(parent, text, settingKey, options, defaultIndex)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 30)
    frame.BackgroundTransparency = 1
    frame.Parent = parent

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.5, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(220, 220, 220)
    label.TextSize = 14
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham
    label.Parent = frame

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 100, 1, -4)
    btn.Position = UDim2.new(1, -100, 0, 2)
    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    btn.Text = options[defaultIndex]
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 14
    btn.Font = Enum.Font.GothamBold
    btn.Parent = frame

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 4)
    btnCorner.Parent = btn

    local currentIndex = defaultIndex
    Settings[settingKey] = options[defaultIndex]

    btn.MouseButton1Click:Connect(function()
        currentIndex = currentIndex % #options + 1
        btn.Text = options[currentIndex]
        Settings[settingKey] = options[currentIndex]
    end)

    return frame
end

CreateToggle(Scroll, "Aimlock", "Aimlock", false)
CreateToggle(Scroll, "FOV Circle", "FOVCircle", false)
CreateToggle(Scroll, "Triggerbot", "Triggerbot", false)
CreateToggle(Scroll, "ESP", "ESP", false)
CreateToggle(Scroll, "ESP Name", "ESPName", false)
CreateToggle(Scroll, "ESP Health", "ESPHealth", false)
CreateToggle(Scroll, "ESP Distance", "ESPDistance", false)
CreateToggle(Scroll, "ESP Team Color", "ESPTeam", false)
CreateToggle(Scroll, "Show Teammates", "ShowTeammates", false)
CreateToggle(Scroll, "Ignore Teammates", "IgnoreTeammates", false)
CreateToggle(Scroll, "Ignore Walls", "IgnoreWalls", false)
CreateSelector(Scroll, "Target Mode", "TargetMode", {"ClosestDistance", "LowestHP", "HighestHP"}, 1)
CreateSelector(Scroll, "Aim Part", "AimPart", {"Head", "Body"}, 1)
CreateToggle(Scroll, "No Recoil", "NoRecoil", false)
CreateToggle(Scroll, "Infinite Ammo", "InfiniteAmmo", false)
CreateToggle(Scroll, "Random Offset", "RandomOffset", false)

local function UpdateCanvas()
    local count = 0
    for _, child in pairs(Scroll:GetChildren()) do
        if child:IsA("Frame") then
            count = count + 1
        end
    end
    Scroll.CanvasSize = UDim2.new(0, 0, 0, count * 35 + 20)
end
UpdateCanvas()
Layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(UpdateCanvas)

ToggleMenuBtn.MouseButton1Click:Connect(function()
    MenuOpen = not MenuOpen
    if MenuOpen then
        Menu.Size = UDim2.new(0, 280, 0, 460)
        Scroll.Visible = true
        ToggleMenuBtn.Text = "_"
    else
        Menu.Size = UDim2.new(0, 280, 0, 30)
        Scroll.Visible = false
        ToggleMenuBtn.Text = "+"
    end
end)

CloseBtn.MouseButton1Click:Connect(function()
    Menu.Visible = false
end)

-- PHAN 6: TAO FOV CIRCLE
FOVCircle = Instance.new("Frame")
FOVCircle.Size = UDim2.new(0, Settings.FOVRadius * 2, 0, Settings.FOVRadius * 2)
FOVCircle.Position = UDim2.new(0.5, -Settings.FOVRadius, 0.5, -Settings.FOVRadius)
FOVCircle.BackgroundTransparency = 1
FOVCircle.BorderSizePixel = 3
FOVCircle.BorderColor3 = Color3.fromRGB(0, 255, 255)
FOVCircle.Visible = false
FOVCircle.Parent = ScreenGui

local FOVCorner = Instance.new("UICorner")
FOVCorner.CornerRadius = UDim.new(1, 0)
FOVCorner.Parent = FOVCircle

FOVGlow = Instance.new("Frame")
FOVGlow.Size = UDim2.new(1, 8, 1, 8)
FOVGlow.Position = UDim2.new(0.5, -4, 0.5, -4)
FOVGlow.AnchorPoint = Vector2.new(0.5, 0.5)
FOVGlow.BackgroundColor3 = Color3.fromRGB(0, 255, 255)
FOVGlow.BackgroundTransparency = 0.8
FOVGlow.BorderSizePixel = 0
FOVGlow.Parent = FOVCircle

local GlowCorner = Instance.new("UICorner")
GlowCorner.CornerRadius = UDim.new(1, 0)
GlowCorner.Parent = FOVGlow

FOVLabel = Instance.new("TextLabel")
FOVLabel.Size = UDim2.new(0, 40, 0, 20)
FOVLabel.Position = UDim2.new(1, 5, 0.5, -10)
FOVLabel.BackgroundTransparency = 1
FOVLabel.Text = tostring(Settings.FOVRadius)
FOVLabel.TextColor3 = Color3.fromRGB(0, 255, 255)
FOVLabel.TextSize = 14
FOVLabel.Font = Enum.Font.GothamBold
FOVLabel.TextXAlignment = Enum.TextXAlignment.Left
FOVLabel.Parent = FOVCircle

-- PHAN 7: HAM UPDATE ESP
local function UpdateESP()
    for _, obj in pairs(ESPCache) do
        if obj and obj.Frame then
            obj.Frame:Destroy()
        end
    end
    ESPCache = {}

    if not Settings.ESP then return end

    for _, plr in pairs(Players:GetPlayers()) do
        if plr == LocalPlayer then continue end
        if not Settings.ShowTeammates and IsTeammate(plr) then continue end
        local character = plr.Character
        if not IsAlive(character) then continue end

        local hrp = character:FindFirstChild("HumanoidRootPart")
        if not hrp then continue end

        local screenPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
        if not onScreen then continue end

        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(0, 200, 0, 60)
        frame.Position = UDim2.new(0, screenPos.X - 100, 0, screenPos.Y - 30)
        frame.BackgroundTransparency = 0.5
        frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        frame.BorderSizePixel = 0
        frame.Parent = ScreenGui

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 4)
        corner.Parent = frame

        local nameLabel = Instance.new("TextLabel")
        nameLabel.Size = UDim2.new(1, 0, 0, 20)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Text = plr.Name
        if Settings.ESPTeam and IsTeammate(plr) then
            nameLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        else
            nameLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
        end
        nameLabel.TextSize = 14
        nameLabel.Font = Enum.Font.GothamBold
        nameLabel.TextXAlignment = Enum.TextXAlignment.Center
        nameLabel.Visible = Settings.ESPName
        nameLabel.Parent = frame

        local healthLabel = Instance.new("TextLabel")
        healthLabel.Size = UDim2.new(1, 0, 0, 18)
        healthLabel.Position = UDim2.new(0, 0, 0, 20)
        healthLabel.BackgroundTransparency = 1
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        healthLabel.Text = "HP: " .. (humanoid and math.floor(humanoid.Health) or "?")
        healthLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        healthLabel.TextSize = 12
        healthLabel.Font = Enum.Font.Gotham
        healthLabel.TextXAlignment = Enum.TextXAlignment.Center
        healthLabel.Visible = Settings.ESPHealth
        healthLabel.Parent = frame

        local distLabel = Instance.new("TextLabel")
        distLabel.Size = UDim2.new(1, 0, 0, 18)
        distLabel.Position = UDim2.new(0, 0, 0, 38)
        distLabel.BackgroundTransparency = 1
        local dist = (Camera.CFrame.Position - hrp.Position).Magnitude
        distLabel.Text = string.format("%.1f m", dist)
        distLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
        distLabel.TextSize = 12
        distLabel.Font = Enum.Font.Gotham
        distLabel.TextXAlignment = Enum.TextXAlignment.Center
        distLabel.Visible = Settings.ESPDistance
        distLabel.Parent = frame

        ESPCache[plr] = {
            Frame = frame,
            Name = nameLabel,
            Health = healthLabel,
            Distance = distLabel,
        }
    end
end

-- PHAN 8: VONG LAP CHINH
RunService.RenderStepped:Connect(function()
    FOVCircle.Visible = Settings.FOVCircle
    FOVGlow.Visible = Settings.FOVCircle
    FOVLabel.Visible = Settings.FOVCircle
    FOVLabel.Text = tostring(Settings.FOVRadius)

    if Settings.NoRecoil then
        NoRecoil()
    end

    if Settings.InfiniteAmmo then
        InfiniteAmmo()
    end

    if Settings.Triggerbot then
        Triggerbot()
    end

    if Settings.Aimlock then
        Target = GetTargets()
        if Target then
            local targetPos = Target.Part + GetRandomOffset()
            local camPos = Camera.CFrame.Position
            Camera.CFrame = Camera.CFrame:Lerp(
                CFrame.new(camPos, targetPos),
                Settings.AimSmoothness
            )
        end
    end

    if Settings.ESP then
        for _, plr in pairs(Players:GetPlayers()) do
            if plr == LocalPlayer then continue end
            if not Settings.ShowTeammates and IsTeammate(plr) then continue end
            local character = plr.Character
            if not IsAlive(character) then continue end

            local hrp = character:FindFirstChild("HumanoidRootPart")
            if not hrp then continue end

            local screenPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
            local obj = ESPCache[plr]
            if obj and onScreen then
                obj.Frame.Position = UDim2.new(0, screenPos.X - 100, 0, screenPos.Y - 30)
                obj.Frame.Visible = true

                if Settings.ESPName then
                    obj.Name.Visible = true
                    if Settings.ESPTeam and IsTeammate(plr) then
                        obj.Name.TextColor3 = Color3.fromRGB(0, 255, 0)
                    else
                        obj.Name.TextColor3 = Color3.fromRGB(255, 0, 0)
                    end
                else
                    obj.Name.Visible = false
                end

                if Settings.ESPHealth then
                    obj.Health.Visible = true
                    local humanoid = character:FindFirstChildOfClass("Humanoid")
                    obj.Health.Text = "HP: " .. (humanoid and math.floor(humanoid.Health) or "?")
                else
                    obj.Health.Visible = false
                end

                if Settings.ESPDistance then
                    obj.Distance.Visible = true
                    local dist = (Camera.CFrame.Position - hrp.Position).Magnitude
                    obj.Distance.Text = string.format("%.1f m", dist)
                else
                    obj.Distance.Visible = false
                end
            elseif obj then
                obj.Frame.Visible = false
            end
        end
    else
        for _, obj in pairs(ESPCache) do
            if obj and obj.Frame then
                obj.Frame.Visible = false
            end
        end
    end
end)

-- PHAN 9: XU LY SU KIEN CHARACTER CHANGED VA KHOI TAO
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    Camera = workspace.CurrentCamera
    UpdateESP()
end)

UpdateESP()
